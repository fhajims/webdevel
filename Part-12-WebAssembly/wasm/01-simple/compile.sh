#!/bin/bash
# Requirements: tsc (TypeScript Compiler)
# e.g. install "globally" (as command line tool) via node package manager
# sudo npm install -g typescript
echo "We compiel TS to JS"
tsc hello42.ts
